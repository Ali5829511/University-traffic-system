# 🔗 دليل التكامل الشامل
## نظام إدارة المرور الجامعي - وحدة إسكان هيئة التدريس
### Integration Guide - University Traffic Management System

---

## 📋 نظرة عامة / Overview

هذا الدليل يوضح كيفية تكامل الأنظمة المختلفة في نظام إدارة المرور الجامعي، بما في ذلك:
- **Plate Recognizer Snapshot Cloud** - للتعرف على لوحات السيارات
- **ParkPow** - لإدارة المواقف والزيارات
- **قاعدة البيانات المحلية (PostgreSQL)** - لحفظ البيانات
- **نظام التقارير** - لإنشاء تقارير رسمية (Excel + PDF + HTML)

---

## 🧩 وصف التكامل / Integration Description

### 1. 📸 مصدر البيانات (Plate Recognizer Snapshot Cloud)

#### الوظيفة:
يتم رفع الصور (من الكاميرات أو الملفات) إلى خدمة Plate Recognizer عبر **API Token**.

#### آلية العمل:
```
[صورة كاميرا/ملف] 
     ↓
[رفع عبر API/FTP]
     ↓
[Plate Recognizer Cloud]
     ↓
[تحليل الصورة واستخراج البيانات]
     ↓
[نتيجة JSON]
```

#### البيانات المستخرجة:
| الحقل | الوصف | مثال |
|-------|-------|------|
| `plate` | رقم اللوحة | أ ب ج 1234 |
| `vehicle.type` | نوع المركبة | Sedan |
| `vehicle.color` | لون المركبة | White |
| `score` | نسبة الدقة | 0.95 |
| `region.code` | رمز المنطقة | sa |

#### مثال على الاستجابة:
```json
{
  "processing_time": 123.45,
  "results": [
    {
      "plate": "أ ب ج 1234",
      "region": { "code": "sa", "score": 0.95 },
      "vehicle": {
        "type": "Sedan",
        "color": [{ "color": "white", "confidence": 0.92 }]
      },
      "score": 0.95
    }
  ]
}
```

#### إعدادات API:
- **API Base URL:** `https://api.platerecognizer.com/v1`
- **Snapshot Endpoint:** `/plate-reader/`
- **Max Calls/Second:** 8
- **Total API Calls:** 50,000/month

---

### 2. 🔄 التوجيه إلى ParkPow

#### الوظيفة:
بعد الحصول على النتيجة من Plate Recognizer، يتم إرسالها مباشرة إلى منصة ParkPow.

#### آلية العمل:
```
[نتيجة Plate Recognizer]
     ↓
[Webhook POST Request]
     ↓
[ParkPow API]
     ↓
[تسجيل الزيارة/المخالفة]
     ↓
[عرض في واجهة Review/Vehicles]
```

#### إعدادات Webhook:
```yaml
Target URL: https://app.parkpow.com/api/v1/webhook-receiver/
Method: POST
Content-Type: application/json
Authorization: Token YOUR_PARKPOW_TOKEN
```

> ⚠️ **تحذير أمني:** لا تقم بحفظ الرموز (Tokens) في ملفات الكود المصدري. استخدم متغيرات البيئة أو ملفات الإعدادات المحمية.

#### الفوائد:
- ✅ تسجيل تلقائي للزيارات
- ✅ تتبع المخالفات في الوقت الفعلي
- ✅ واجهة مراجعة مركزية
- ✅ أرشفة رسمية للبيانات

---

### 3. 💾 قاعدة البيانات المحلية (PostgreSQL)

#### الوظيفة:
حفظ نفس البيانات محلياً للتحكم الكامل والربط مع أنظمة أخرى.

#### الجداول الرئيسية:

##### جدول الزيارات (`visits`)
```sql
CREATE TABLE visits (
    id SERIAL PRIMARY KEY,
    license_plate VARCHAR(50),      -- رقم اللوحة
    type VARCHAR(50),               -- نوع المركبة
    color VARCHAR(50),              -- لون المركبة
    site VARCHAR(100),              -- الموقع
    entrance_ts TIMESTAMP,          -- تاريخ ووقت الدخول
    image_path VARCHAR(500),        -- مسار الصورة المحلية
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

##### جدول المخالفات (`traffic_violations`)
```sql
CREATE TABLE traffic_violations (
    id SERIAL PRIMARY KEY,
    violation_number VARCHAR(50),   -- رقم المخالفة
    plate_number VARCHAR(20),       -- رقم اللوحة
    violation_type VARCHAR(100),    -- نوع المخالفة
    violation_date DATE,            -- تاريخ المخالفة
    violation_time TIME,            -- وقت المخالفة
    location VARCHAR(200),          -- الموقع
    evidence_image VARCHAR(500),    -- رابط صورة الدليل
    source VARCHAR(30),             -- المصدر (plate_recognizer/يدوي)
    confidence_score DECIMAL(5,2),  -- نسبة الدقة
    violation_status VARCHAR(30),   -- الحالة (جديد/مؤكد/مدفوع)
    fine_amount DECIMAL(10,2),      -- مبلغ الغرامة
    is_paid BOOLEAN,                -- هل تم الدفع
    officer_name VARCHAR(100),      -- اسم الضابط
    notes TEXT,                     -- ملاحظات
    created_at TIMESTAMP            -- تاريخ الإنشاء
);
```

#### الحقول المخزنة:
| الحقل | الوصف |
|-------|-------|
| رقم اللوحة | النص الكامل للوحة |
| التاريخ والوقت | timestamp دقيق |
| الكاميرا/الموقع | معرف الكاميرا أو الموقع |
| رابط الصورة | رابط الصورة الأصلية |
| رابط المصغّر | رابط الصورة المصغّرة |
| نسبة الدقة | مستوى الثقة في التعرف |

---

### 4. 🖼️ إدارة الصور

#### البنية:
```
data/
├── images/           # الصور الأصلية
│   ├── ABC123.jpg
│   ├── XYZ789.jpg
│   └── ...
├── thumbs/           # الصور المصغّرة
│   ├── ABC123_thumb.jpg
│   ├── XYZ789_thumb.jpg
│   └── ...
└── results/          # التقارير الناتجة
    ├── تقرير_الزيارات_20251125.xlsx
    ├── تقرير_الزيارات_20251125.pdf
    └── ...
```

#### معالجة الصور:
1. **التحميل:** تحميل الصورة من ParkPow أو Plate Recognizer
2. **الحفظ:** حفظ الصورة الأصلية في مجلد `images/`
3. **التصغير:** إنشاء نسخة مصغّرة في `thumbs/`
4. **الربط:** ربط مسار الصورة بالسجل في قاعدة البيانات

#### مواصفات الصور المصغّرة:
- **الأبعاد:** 80x60 بكسل
- **الصيغة:** JPEG
- **الجودة:** 80%

---

### 5. 📊 التقارير الرسمية

#### أنواع التقارير:

##### تقرير Excel
- 📈 جدول منظم مع جميع البيانات
- 🖼️ إشارة لوجود الصور
- 📋 أعمدة قابلة للتصفية والفرز
- 📁 مسار الصورة لكل سجل

##### تقرير PDF
- 📄 تقرير رسمي جاهز للطباعة
- 🖼️ صور مصغّرة مدمجة
- 🏛️ شعار المؤسسة
- ✅ صالح للأرشفة القضائية
- 📝 مكان للختم والتوقيع

##### تقرير HTML
- 🌐 نسخة أرشيفية قابلة للعرض في المتصفح
- 📧 يمكن إرسالها بالبريد الإلكتروني
- 🖼️ صور مصغّرة مضمّنة
- 🎨 تصميم احترافي

#### محتويات التقرير:
| العنصر | الوصف |
|--------|-------|
| رأس التقرير | شعار + عنوان + تاريخ |
| ملخص | إجمالي السجلات + إحصائيات |
| الجدول | البيانات مع الصور المصغّرة |
| التذييل | توقيع + ختم + معلومات النظام |

---

### 6. ⚙️ الأتمتة (Automation Scripts)

#### سكريبتات Node.js:

##### استيراد الزيارات مع الصور وتوليد PDF
```bash
npm run import-visits
# أو
node jobs/import_visits_with_images_and_pdf.js [path/to/visits.csv]
```

**الوظائف:**
1. قراءة ملف CSV
2. تحميل الصور من الروابط
3. حفظ البيانات في قاعدة البيانات
4. إنشاء تقرير Excel
5. إنشاء تقرير PDF رسمي

##### استيراد الملصقات
```bash
npm run import-stickers
```

##### استيراد جميع البيانات
```bash
npm run import-all
```

#### سكريبتات الخادم:

##### تشغيل الخادم
```bash
npm start
# أو
node server.js
```

##### إعداد قاعدة البيانات
```bash
npm run setup
```

---

## 🔗 مخطط التدفق الكامل / Complete Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        📷 الكاميرا / مصدر الصورة                         │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    📤 رفع الصورة (API/FTP)                              │
│                                                                          │
│  • API Token: للتحقق من الهوية                                          │
│  • Endpoint: /plate-reader/                                              │
│  • Regions: sa (السعودية)                                               │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│               🔍 Plate Recognizer Snapshot Cloud                         │
│                                                                          │
│  • تحليل الصورة بالذكاء الاصطناعي                                       │
│  • استخراج: رقم اللوحة، النوع، اللون، الدقة                            │
│  • إرجاع نتيجة JSON                                                     │
└────────────────┬────────────────────────────────────┬───────────────────┘
                 │                                    │
                 ▼                                    ▼
┌────────────────────────────────┐    ┌────────────────────────────────────┐
│       🔄 Webhook إلى ParkPow   │    │      💾 قاعدة البيانات المحلية     │
│                                │    │                                    │
│  • تسجيل تلقائي للزيارة       │    │  • حفظ في جدول visits              │
│  • ربط مع حساب ParkPow        │    │  • حفظ في جدول violations          │
│  • عرض في واجهة Review        │    │  • تحميل وحفظ الصور               │
└────────────────┬───────────────┘    └──────────────┬─────────────────────┘
                 │                                    │
                 └──────────────┬─────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    📊 نظام التقارير والأرشفة                            │
│                                                                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                      │
│  │   Excel     │  │    PDF      │  │    HTML     │                      │
│  │  جداول     │  │  رسمي       │  │  أرشيفي     │                      │
│  │  بيانات    │  │  للطباعة    │  │  للبريد     │                      │
│  └─────────────┘  └─────────────┘  └─────────────┘                      │
│                                                                          │
│  • صور مصغّرة مدمجة                                                     │
│  • شعار المؤسسة                                                         │
│  • صالح للأرشفة القضائية                                               │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🛡️ الفوائد / Benefits

### الشفافية
- ✅ كل عملية مسجلة رسمياً مع صورة
- ✅ سجل كامل للأنشطة (Audit Logs)
- ✅ تتبع مصدر كل مخالفة

### الأمان
- ✅ الرموز (Tokens) محدثة ومحفوظة بشكل آمن
- ✅ اتصالات HTTPS مشفرة
- ✅ التحقق من هوية المستخدمين

### قابلية الطباعة
- ✅ تقارير PDF رسمية جاهزة للرفع القضائي
- ✅ صور مصغّرة مدمجة في التقارير
- ✅ مكان للختم والتوقيع

### الأتمتة
- ✅ لا حاجة للعمل اليدوي في ربط الصور
- ✅ إنشاء التقارير تلقائياً
- ✅ تحديث البيانات في الوقت الفعلي

---

## ⚙️ إعداد التكامل / Integration Setup

### المتطلبات:
1. حساب Plate Recognizer مع API Token
2. حساب ParkPow مع Token
3. قاعدة بيانات PostgreSQL
4. Node.js v18+

### خطوات الإعداد:

#### 1. إعداد متغيرات البيئة
```bash
cp .env.example .env
# عدّل ملف .env وأضف:
# DATABASE_URL=postgresql://user:password@host:port/database
```

#### 2. إعداد Plate Recognizer
- تحديث `plate_recognizer_config.json` بالرموز الصحيحة

#### 3. إعداد Webhook في Plate Recognizer
- من لوحة التحكم → Webhooks → Add New
- Target URL: `https://app.parkpow.com/api/v1/webhook-receiver/`
- Authorization: `Token YOUR_PARKPOW_TOKEN`

#### 4. تشغيل النظام
```bash
npm install
npm run setup
npm start
```

---

## 📁 الملفات ذات الصلة / Related Files

| الملف | الوصف |
|-------|-------|
| `plate_recognizer_config.json` | إعدادات Plate Recognizer و ParkPow |
| `jobs/import_visits_with_images_and_pdf.js` | سكريبت استيراد الزيارات |
| `server.js` | الخادم الرئيسي مع API endpoints |
| `schema.postgres.sql` | مخطط قاعدة البيانات |
| `reports/templates/report_with_thumbs.html` | قالب تقرير HTML |
| `API_SETUP.md` | دليل إعداد Plate Recognizer API |
| `WEBHOOK_ARCHITECTURE.md` | بنية Webhook التفصيلية |

---

## 🚀 أدوات التعرف على اللوحات المدمجة / Integrated ALPR Tools

تم دمج مستودع [deep-license-plate-recognition](https://github.com/parkpow/deep-license-plate-recognition) في هذا المشروع ضمن المجلد `deep-license-plate-recognition/`:

### السكريبتات الرئيسية / Main Scripts

| الملف | الوصف |
|-------|-------|
| `deep-license-plate-recognition/plate_recognition.py` | قراءة لوحات السيارات من الصور |
| `deep-license-plate-recognition/number_plate_redaction.py` | طمس/إخفاء اللوحات في الصور |
| `deep-license-plate-recognition/ftp_processor.py` | معالجة صور FTP |
| `deep-license-plate-recognition/benchmark.py` | قياس أداء SDK |
| `deep-license-plate-recognition/transfer.py` | نقل الصور تلقائياً |

### مجلدات الأدوات / Tools Directories

| المجلد | الوصف |
|--------|-------|
| `deep-license-plate-recognition/webhooks/` | أدوات استقبال البيانات عبر Webhook |
| `deep-license-plate-recognition/stream/` | أدوات البث المباشر ومراقبة الكاميرات |
| `deep-license-plate-recognition/docker/` | أدوات Docker للتشغيل المحلي |
| `deep-license-plate-recognition/cpp/` | مثال C++ |
| `deep-license-plate-recognition/csharp/` | مثال C# |
| `deep-license-plate-recognition/java/` | مثال Java |

### استخدام الأدوات / Using the Tools

#### قراءة لوحة من صورة:
```bash
cd deep-license-plate-recognition
pip install requests pillow
python plate_recognition.py --api-key MY_API_KEY /path/to/vehicle.jpg
```

#### معالجة صور FTP:
```bash
python ftp_processor.py --api-key MY_API_KEY --ftp-host hostname --ftp-user user --ftp-password password --folder /path/to/folder
```

#### مراقبة دفق الكاميرا:
```bash
cd stream
python stream_monitor.py -c stream_container_name -p 8001
```

---

## 🔗 روابط مفيدة / Useful Links

- 📖 [Plate Recognizer Documentation](https://docs.platerecognizer.com)
- 🔧 [Plate Recognizer Dashboard](https://app.platerecognizer.com)
- 🅿️ [ParkPow Dashboard](https://app.parkpow.com)
- 📊 [ParkPow API](https://app.parkpow.com/api/v1/webhook-receiver/)
- 📚 [Deep License Plate Recognition GitHub](https://github.com/parkpow/deep-license-plate-recognition)

---

## 📞 الدعم / Support

للحصول على المساعدة:
1. راجع ملفات التوثيق في المشروع
2. تحقق من `SECURITY.md` للأمان
3. راجع `API_SETUP.md` للإعدادات التفصيلية
4. راجع `WEBHOOK_ARCHITECTURE.md` لبنية Webhook
5. راجع `deep-license-plate-recognition/README.md` لأدوات التعرف على اللوحات

---

**تم إنشاء هذا الدليل بواسطة:** نظام إدارة المرور الجامعي  
**جامعة الإمام محمد بن سعود الإسلامية**  
**التاريخ:** 2025-11-30  
**الإصدار:** 1.1
