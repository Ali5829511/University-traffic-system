# 🔒 تنبيه أمني - Security Advisory
# University Traffic Management System

**رقم التنبيه / Advisory ID:** SA-2025-001  
**التاريخ / Date:** 2025-12-02  
**الخطورة / Severity:** ⚠️ HIGH  
**الحالة / Status:** مراقب / MONITORED

---

## 📋 ملخص / Summary

تم اكتشاف ثغرات أمنية معروفة في مكتبة `xlsx` المستخدمة في نظام التصدير. هذا التنبيه يوضح طبيعة الثغرات والتدابير المتخذة والتوصيات.

A known security vulnerability has been identified in the `xlsx` library used in the export system. This advisory explains the nature of the vulnerabilities, measures taken, and recommendations.

---

## 🔍 تفاصيل الثغرة / Vulnerability Details

### المكتبة المتأثرة / Affected Library
```
Package: xlsx
Current Version: 0.18.5
Vulnerabilities: 2 (HIGH severity)
```

### الثغرة الأولى: Prototype Pollution
- **المعرف / ID:** GHSA-4r6h-8v6p-xvw6
- **الخطورة / Severity:** HIGH (CVSS 7.8)
- **الوصف:** إمكانية تلويث النموذج الأولي (Prototype Pollution)
- **الإصدارات المتأثرة:** < 0.19.3
- **الإصدارات الآمنة:** لا يوجد حالياً / Not available yet

**التفاصيل التقنية:**
```
CVSS Vector: CVSS:3.1/AV:L/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:H
CWE: CWE-1321 (Improperly Controlled Modification of Object Prototype)
```

### الثغرة الثانية: Regular Expression Denial of Service (ReDoS)
- **المعرف / ID:** GHSA-5pgg-2g8v-p4x9
- **الخطورة / Severity:** HIGH (CVSS 7.5)
- **الوصف:** إمكانية رفض الخدمة عبر تعبيرات نمطية
- **الإصدارات المتأثرة:** < 0.20.2
- **الإصدارات الآمنة:** لا يوجد حالياً / Not available yet

**التفاصيل التقنية:**
```
CVSS Vector: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:H
CWE: CWE-1333 (Inefficient Regular Expression Complexity)
```

---

## 📊 تقييم المخاطر / Risk Assessment

### احتمالية الاستغلال / Exploitation Likelihood: منخفضة / LOW

**الأسباب:**

1. **المصادقة المطلوبة / Authentication Required**
   - يتطلب النظام تسجيل دخول للوصول إلى نظام التصدير
   - فقط المستخدمين المصرح لهم يمكنهم استخدام وظيفة التصدير

2. **محدودية نقاط الهجوم / Limited Attack Surface**
   - الثغرة تتطلب رفع ملف Excel ضار
   - النظام الحالي لا يقبل رفع ملفات Excel من المستخدمين
   - xlsx تُستخدم فقط للتصدير (الكتابة)، وليس للقراءة

3. **التحقق من المدخلات / Input Validation**
   - جميع المدخلات يتم التحقق منها
   - نظام multer يفحص أنواع الملفات المرفوعة
   - حد أقصى لحجم الملفات: 5MB

### التأثير المحتمل / Potential Impact: متوسط / MEDIUM

إذا تم استغلال الثغرة بنجاح:
- قد يتمكن المهاجم من تنفيذ كود JavaScript ضار
- إمكانية التأثير على سلامة البيانات
- احتمال رفض الخدمة (DoS)

**لكن:** التأثير محدود بسبب:
- الحاجة إلى صلاحيات مستخدم مصادق
- محدودية الوصول إلى وظيفة استيراد Excel

---

## 🛡️ التدابير المطبقة / Implemented Mitigations

### 1. قيود الوصول / Access Controls
```javascript
// في جميع صفحات التصدير
checkUserAuthentication(); // التحقق من تسجيل الدخول
checkUserRole(['admin', 'violations']); // التحقق من الصلاحيات
```

### 2. التحقق من الملفات / File Validation
```javascript
// في server.js
const multer = require('multer');
const storage = multer.diskStorage({ /* تكوين التخزين */ });

const upload = multer({
    storage: storage,
    limits: { fileSize: 5 * 1024 * 1024 }, // 5MB
    fileFilter: function (req, file, cb) {
        const imageTypes = /jpeg|jpg|png|gif/;
        const ext = path.extname(file.originalname).toLowerCase();
        
        // فقط الصور مسموح بها حالياً
        if (imageTypes.test(ext) && imageTypes.test(file.mimetype)) {
            return cb(null, true);
        }
        cb(new Error('نوع الملف غير مسموح به'));
    }
});
```

### 3. Rate Limiting
```javascript
// تحديد عدد الطلبات
const limiter = rateLimit({
    windowMs: 15 * 60 * 1000, // 15 دقيقة
    max: 100 // 100 طلب كحد أقصى
});
```

### 4. سجلات التدقيق / Audit Logs
```sql
-- جدول audit_logs يسجل جميع العمليات
CREATE TABLE audit_logs (
    id SERIAL PRIMARY KEY,
    user_id INTEGER,
    action VARCHAR(50),
    ip_address VARCHAR(50),
    user_agent TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 📋 خطة العمل / Action Plan

### قصيرة المدى (فوري) / Short-term (Immediate)

- [x] توثيق الثغرة وتقييم المخاطر
- [x] التأكد من تطبيق جميع التدابير الوقائية
- [x] إنشاء تنبيه أمني
- [ ] تدريب المستخدمين على عدم رفع ملفات من مصادر غير موثوقة

### متوسطة المدى (شهر واحد) / Medium-term (1 month)

- [ ] مراقبة تحديثات xlsx أسبوعياً
- [ ] اختبار البدائل المحتملة:
  - ExcelJS (أكثر أماناً وموثوقية)
  - xlsx-populate (بديل آخر)
  - node-xlsx (بسيط وخفيف)
- [ ] إنشاء اختبارات أمنية للتصدير

### طويلة المدى (3 أشهر) / Long-term (3 months)

- [ ] الانتقال إلى مكتبة بديلة آمنة
- [ ] تحديث جميع التوثيق
- [ ] اختبار شامل للنظام الجديد

---

## 🔄 البدائل المقترحة / Suggested Alternatives

### الخيار 1: ExcelJS (موصى به / Recommended)

**المزايا:**
- ✅ أكثر أماناً من xlsx
- ✅ دعم أفضل وأكثر نشاطاً
- ✅ ميزات متقدمة (تنسيق، صيغ، صور)
- ✅ TypeScript support

**العيوب:**
- ⚠️ حجم أكبر قليلاً
- ⚠️ يحتاج إلى تعديل الكود الحالي

**الاستخدام:**
```javascript
const ExcelJS = require('exceljs');
const workbook = new ExcelJS.Workbook();
const worksheet = workbook.addWorksheet('My Sheet');

worksheet.addRow(['ID', 'Name', 'Email']);
worksheet.addRow([1, 'John', 'john@example.com']);

await workbook.xlsx.writeFile('output.xlsx');
```

### الخيار 2: xlsx-populate

**المزايا:**
- ✅ API بسيط وسهل
- ✅ حجم صغير
- ✅ أداء جيد

**العيوب:**
- ⚠️ ميزات أقل من ExcelJS

### الخيار 3: الإبقاء على xlsx مع المراقبة

**المزايا:**
- ✅ لا يحتاج تعديلات
- ✅ يعمل بشكل جيد حالياً

**العيوب:**
- ⚠️ الثغرات الأمنية موجودة
- ⚠️ يحتاج مراقبة مستمرة

---

## 📝 توصيات للمستخدمين / User Recommendations

### للمديرين / For Administrators

1. **مراقبة سجلات التدقيق**
   - راجع سجلات التدقيق يومياً
   - ابحث عن أنماط غير عادية في عمليات التصدير

2. **إدارة الصلاحيات**
   - قيّد الوصول إلى نظام التصدير
   - امنح الصلاحيات فقط للمستخدمين الموثوقين

3. **النسخ الاحتياطي**
   - نفذ نسخ احتياطي يومي
   - اختبر استعادة البيانات بانتظام

### للمستخدمين / For Users

1. **لا ترفع ملفات Excel من مصادر غير موثوقة**
2. **استخدم كلمات مرور قوية**
3. **أبلغ عن أي سلوك غير طبيعي**

---

## 📞 الإبلاغ عن المشاكل / Reporting Issues

إذا اكتشفت:
- سلوك غير طبيعي في نظام التصدير
- محاولات استغلال محتملة
- ثغرات أمنية جديدة

**اتصل فوراً بـ:**
- مدير النظام
- فريق الأمن المعلوماتي

**لا تحاول:**
- اختبار الثغرات بنفسك
- مشاركة معلومات الثغرات علناً

---

## 📚 مراجع / References

1. **GitHub Advisory Database**
   - [GHSA-4r6h-8v6p-xvw6](https://github.com/advisories/GHSA-4r6h-8v6p-xvw6)
   - [GHSA-5pgg-2g8v-p4x9](https://github.com/advisories/GHSA-5pgg-2g8v-p4x9)

2. **Common Weakness Enumeration (CWE)**
   - [CWE-1321: Improperly Controlled Modification of Object Prototype](https://cwe.mitre.org/data/definitions/1321.html)
   - [CWE-1333: Inefficient Regular Expression Complexity](https://cwe.mitre.org/data/definitions/1333.html)

3. **CVSS Calculator**
   - [CVSS v3.1 Calculator](https://www.first.org/cvss/calculator/3.1)

---

## ✅ الخلاصة / Conclusion

**الحالة الحالية / Current Status:**
- ⚠️ الثغرة موجودة لكن محمية بتدابير متعددة
- ✅ احتمالية الاستغلال منخفضة
- ✅ التأثير محدود
- ✅ النظام آمن للاستخدام مع المراقبة

**التوصية / Recommendation:**
```
✅ النظام آمن للنشر والاستخدام
✅ System is safe for deployment and use

مع:
- مراقبة تحديثات xlsx
- تطبيق جميع التدابير الوقائية
- النظر في الانتقال إلى ExcelJS في المستقبل
```

---

**آخر تحديث / Last Updated:** 2025-12-02  
**المراجعة القادمة / Next Review:** 2025-12-09  
**المسؤول / Responsible:** Security Team

---

**نهاية التنبيه الأمني / End of Security Advisory**
