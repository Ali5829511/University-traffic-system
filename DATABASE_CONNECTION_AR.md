# 🔗 دليل ربط النظام بقاعدة البيانات السحابية

## المتطلبات الأساسية

1. **Node.js** (الإصدار 16 أو أحدث)
2. **قاعدة بيانات PostgreSQL سحابية** (مثل Supabase أو Neon)
3. **معرفة أساسية بسطر الأوامر**

---

## الخطوات التفصيلية

### الخطوة 1️⃣: تثبيت Node.js

إذا لم يكن لديك Node.js:

**Windows:**
- حمّل من [nodejs.org](https://nodejs.org)
- شغّل الملف وأكمل التثبيت

**Mac:**
```bash
brew install node
```

**Linux:**
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

للتحقق من التثبيت:
```bash
node --version
npm --version
```

---

### الخطوة 2️⃣: إنشاء قاعدة بيانات سحابية

#### الخيار أ: Supabase (موصى به - مجاني)

1. **إنشاء حساب:**
   - اذهب إلى https://supabase.com
   - اضغط "Start your project"
   - سجّل الدخول بحساب GitHub

2. **إنشاء مشروع جديد:**
   - اضغط "New Project"
   - أدخل اسم المشروع
   - اختر كلمة مرور قوية (احفظها!)
   - اختر المنطقة الأقرب لك
   - اضغط "Create new project"
   - انتظر 2-3 دقائق حتى يكتمل الإعداد

3. **الحصول على Connection String:**
   - اذهب إلى "Project Settings" (الإعدادات)
   - اختر "Database" من القائمة الجانبية
   - انزل إلى "Connection String"
   - اختر "URI" من التبويبات
   - انسخ الرابط (سيكون بالشكل):
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.xxxxxxxxxxxx.supabase.co:5432/postgres
   ```
   - **مهم:** استبدل `[YOUR-PASSWORD]` بكلمة المرور التي اخترتها

#### الخيار ب: Neon (بديل مجاني)

1. اذهب إلى https://neon.tech
2. سجّل الدخول بحساب GitHub
3. اضغط "Create a project"
4. انسخ Connection String من Dashboard

---

### الخطوة 3️⃣: تثبيت المتطلبات

افتح Terminal/Command Prompt في مجلد المشروع:

```bash
# على Windows
cd C:\path\to\University-traffic-system

# على Mac/Linux
cd /path/to/University-traffic-system

# تثبيت الحزم المطلوبة
npm install
```

انتظر حتى يكتمل التثبيت (قد يأخذ 1-2 دقيقة)

---

### الخطوة 4️⃣: إعداد ملف البيئة

1. **إنشاء ملف .env:**

   **على Windows:**
   ```cmd
   copy .env.example .env
   notepad .env
   ```

   **على Mac/Linux:**
   ```bash
   cp .env.example .env
   nano .env
   ```

2. **تعديل الملف:**
   
   افتح ملف `.env` وعدّل السطر التالي:
   ```env
   DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.xxxxxxxxxxxx.supabase.co:5432/postgres
   ```
   
   استبدل الرابط بالرابط الذي حصلت عليه من Supabase أو Neon

3. **احفظ الملف**

---

### الخطوة 5️⃣: تهيئة قاعدة البيانات

الآن قم بتشغيل سكريبت الإعداد:

```bash
npm run setup
```

سيسألك السكريبت عدة أسئلة:

```
Do you want to initialize the database schema? (yes/no): 
```
اكتب: **yes** واضغط Enter

```
Do you want to create default users? (yes/no):
```
اكتب: **yes** واضغط Enter

إذا سارت الأمور بشكل صحيح، سترى:
```
✅ Setup completed successfully!
```

---

### الخطوة 6️⃣: تشغيل الخادم

الآن شغّل الخادم:

```bash
npm start
```

يجب أن ترى رسالة مثل:
```
╔════════════════════════════════════════════╗
║  🎓 University Traffic Management System  ║
║  نظام إدارة المرور الجامعي               ║
╠════════════════════════════════════════════╣
║  Server running on: http://localhost:3000 ║
╚════════════════════════════════════════════╝
```

---

### الخطوة 7️⃣: فتح النظام في المتصفح

افتح المتصفح واذهب إلى:
```
http://localhost:3000
```

سترى صفحة تسجيل الدخول

---

## 🔑 بيانات الدخول الافتراضية

بعد إكمال الإعداد، يمكنك تسجيل الدخول بالحسابات التالية:

### المدير (Admin)
- **اسم المستخدم:** admin
- **كلمة المرور:** admin123

### مسجل المخالفات
- **اسم المستخدم:** violations_officer
- **كلمة المرور:** officer123

### موظف الاستعلام
- **اسم المستخدم:** inquiry_user
- **كلمة المرور:** inquiry123

⚠️ **مهم:** غيّر كلمات المرور هذه في بيئة الإنتاج!

---

## ❓ استكشاف الأخطاء

### مشكلة: "DATABASE_URL not found"

**الحل:**
1. تأكد من إنشاء ملف `.env`
2. تأكد من كتابة `DATABASE_URL=` بشكل صحيح
3. تأكد من عدم وجود مسافات إضافية

---

### مشكلة: "Connection failed"

**الأسباب المحتملة:**

1. **كلمة المرور خاطئة:**
   - تأكد من كتابة كلمة المرور بشكل صحيح في CONNECTION STRING
   - إذا كانت كلمة المرور تحتوي على رموز خاصة، قد تحتاج إلى URL encoding

2. **الاتصال بالإنترنت:**
   - تأكد من اتصالك بالإنترنت

3. **IP Address محظور:**
   - في Supabase: اذهب إلى Settings → Database → اضغط "Add new allowed IP"
   - أضف `0.0.0.0/0` للسماح لجميع العناوين (للتطوير فقط)

---

### مشكلة: "Port 3000 already in use"

**الحل:**
```bash
# أوقف أي تطبيق يستخدم المنفذ 3000
# أو غيّر المنفذ في .env:
PORT=3001
```

---

### مشكلة: "Module not found"

**الحل:**
```bash
# أعد تثبيت الحزم
rm -rf node_modules
npm install
```

---

## 📱 للاستخدام من أجهزة أخرى على نفس الشبكة

1. اكتشف IP Address لجهازك:

   **Windows:**
   ```cmd
   ipconfig
   ```
   ابحث عن "IPv4 Address"

   **Mac/Linux:**
   ```bash
   ifconfig
   # أو
   ip addr show
   ```

2. على الجهاز الآخر، افتح:
   ```
   http://YOUR_IP_ADDRESS:3000
   ```

---

## 🚀 النشر على الإنترنت

بعد تجربة النظام محلياً، يمكنك نشره على الإنترنت:

### على Render.com (مجاني)

1. اذهب إلى https://render.com
2. سجّل الدخول بحساب GitHub
3. اضغط "New +" → "Web Service"
4. اختر repository الخاص بك
5. املأ البيانات:
   - **Name:** university-traffic-system
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
6. أضف Environment Variable:
   - **Key:** `DATABASE_URL`
   - **Value:** [رابط قاعدة البيانات من Supabase]
7. اضغط "Create Web Service"

سيكون موقعك متاح على:
```
https://your-app-name.onrender.com
```

---

## 📊 التحقق من عمل قاعدة البيانات

يمكنك التحقق من عمل قاعدة البيانات بزيارة:
```
http://localhost:3000/api/health
```

يجب أن ترى:
```json
{
  "status": "OK",
  "timestamp": "2024-xx-xx...",
  "database": "connected"
}
```

---

## 🔒 نصائح أمنية

1. **لا تشارك ملف .env أبداً**
2. **غيّر كلمات المرور الافتراضية**
3. **استخدم HTTPS في الإنتاج**
4. **فعّل النسخ الاحتياطي التلقائي للبيانات**
5. **حدد IP Addresses المسموح لها بالوصول**

---

## 📞 الدعم

إذا واجهت أي مشاكل:

1. تحقق من الأخطاء في Terminal
2. تحقق من Console في المتصفح (F12)
3. راجع قسم استكشاف الأخطاء في هذا الدليل
4. راجع ملف `CLOUD_DATABASE_GUIDE.md` للمزيد من التفاصيل

---

**ملاحظة:** هذا النظام جاهز الآن للاستخدام مع قاعدة بيانات سحابية حقيقية بدلاً من localStorage! 🎉
